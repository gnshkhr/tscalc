import utilities from '../../utilities';
import { Maybe } from '../../../monads';

import getComputedState from './getcomputedstate';

const { fakeServices, nonZeroDigitToString } = utilities;

describe('#getComputedState state handler helper', () => {
  describe('passing AccumulatorState with undefined pendingOperation', () => {
    describe('addition', () => {
      it('returns expected ComputedState via service', () => {
        pending();
      });
    });

    describe('subtraction', () => {
      it('returns expected ComputedState via service', () => {
        pending();
      });
    });

    describe('multiplication', () => {
      it('returns expected ComputedState via service', () => {
        pending();
      });
    });

    describe('division', () => {
      it('returns expected ComputedState via service', () => {
        pending();
      });
    });
  });

  describe('passing AccumulatorState with defined pendingOperation', () => {
    describe('addition', () => {
      it('returns expected ComputedState via service', () => {
        pending();
      });
    });

    describe('subtraction', () => {
      it('returns expected ComputedState via service', () => {
        pending();
      });
    });

    describe('multiplication', () => {
      it('returns expected ComputedState via service', () => {
        pending();
      });
    });

    describe('division', () => {
      it('returns expected ComputedState via service', () => {
        pending();
      });
    });
  });
});
